# frozen_string_literal: true

source 'https://rubygems.org'

gem 'cairo'
gem 'pango'
gem 'poppler', '>= 0.9.5'
# rake is required to install poppler.
gem 'rake'

group :development do
  gem 'rubocop'
end
